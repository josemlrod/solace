## Notes

Thank you very much for the opportunity! It's my first time interacting with a NextJS project, and so before starting the assignment I tried my best to get familiarized with the basics.

## Todo:

- It's important to fetch data from the DB instead of using the in-memory array we currently have for the `advocates`
- Add a better fallback to the `Suspense` bondary that wraps the `AdvocatesTable` component
- Ensure we add responsive design for users accessing from mobile devices
- We could add keyboard shortcuts to facilitate search
- We should add the brand colors to the styling
